services:
  - name: web
    env:
      PORT: 5000
      SQLALCHEMY_DATABASE_URI: sqlite:///app.db  # Пример URI для SQLite
    buildCommand: python manage.py collectstatic
    startCommand: gunicorn -w 4 -k gevent -b 5000 run:app

